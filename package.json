{
  "name": "login",
  "version": "1.0.0",
  "description": "This is a simple Task Management Application built with Angular.js for the frontend and Node.js with MongoDB for the backend.",
  "main": "login.js",
  "scripts": {
    "build": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.6.6",
    "node": "^20.7.0"
  },
  "devDependencies": {
    "@angular/cli": "^17.3.0"
  }
}
